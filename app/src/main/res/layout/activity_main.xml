<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="kr.co.google.nougat.fix.fixme.MainActivity">

    <ImageView
        android:id="@+id/splash_logo"
        android:layout_alignParentTop="true"
        android:layout_width="@dimen/logo_width"
        android:layout_height="@dimen/logo_height"
        android:scaleType="centerInside"
        android:src="@drawable/fixme_splash"/>

    <Button
        android:id="@+id/connServer"
        android:layout_toRightOf="@id/splash_logo"
        android:layout_alignTop="@id/splash_logo"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:text="@string/txt_conn_server"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/loadExtLib"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:text="@string/txt_load_expansion"
        android:layout_toRightOf="@id/splash_logo"
        android:layout_alignBottom="@id/splash_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />


    <ListView
        android:id="@+id/list"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:layout_below="@id/splash_logo"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </ListView>

</RelativeLayout>
